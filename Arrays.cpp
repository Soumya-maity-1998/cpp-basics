#include <iostream> 
#include <cmath> 

using namespace std;

int main()

{

    int luckynumbers[] = {4, 8, 15, 16, 23, 42}; 
    int list[20]; 
    list[0] = 5; 

    cout << luckynumbers[0] << endl;

    luckynumbers[0] = 19;  
    cout << luckynumbers[0]; 

    return 0;
}